# Core Flask dependencies
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Caching==2.1.0

# Database
# psycopg2-binary can fail to load DLLs on Windows; psycopg v3 binary wheels are self-contained.
psycopg[binary]==3.2.13
# SQLAlchemy >=2.0.41 adds Python 3.14 support.
SQLAlchemy==2.0.45
alembic==1.13.3

# Pagination
flask-paginate==2024.4.12

# HTTP requests
requests==2.32.3
urllib3==2.2.3
certifi==2024.8.30
charset-normalizer==3.4.0
idna==3.10

# Environment and configuration
python-dotenv==1.0.1

# Template engine
Jinja2==3.1.4
MarkupSafe==3.0.2

# WSGI utilities
Werkzeug==3.0.4
itsdangerous==2.2.0
click==8.1.7

# Background tasks and utilities
blinker==1.8.2
# Python 3.14 on Windows needs a newer greenlet wheel; 3.1.1 fails to build.
greenlet==3.3.0
Mako==1.3.6
# Keep compatible with other modern deps (e.g., pydantic on Python 3.14).
typing_extensions==4.15.0
colorama==0.4.6

# Optional: Performance monitoring
psutil==5.9.8

# Caching (optional Redis support)
redis==5.0.8

# Development dependencies (comment out for production)
# pytest==8.0.0
# pytest-flask==1.3.0
# coverage==7.4.0